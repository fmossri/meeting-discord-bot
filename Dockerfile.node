FROM node:20-alpine

WORKDIR /app

# Install production dependencies
COPY package.json package-lock.json* ./
RUN npm ci --only=production

# Copy source
COPY . .

# Default to production; can be overridden by compose/env
ENV NODE_ENV=production

CMD ["node", "index.js"]

