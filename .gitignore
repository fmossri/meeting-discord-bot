# Dependencies
node_modules/
.venv/

# Secrets and local config
.env
.env.local

# tsummix single-instance lock (scripts/tsummix.js)
.tsummix.lock

# Rotated logs (keep logs/.gitkeep)
logs/*.log
logs/*.log.*

# Editor / IDE / OS
.cursor/
.cursorrules
.DS_Store
Thumbs.db

# STT: downloaded model cache (default: .models/)
.models/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.eggs/
repl.py

# Test / generated assets (large or machine-specific)
tests/audio-samples/
services/transcript-worker/transcripts/*.jsonl
services/transcript-worker/transcripts/*.tmp
reports/*.md

# Optional: comment out if you want to track docs and eslint config in git
docs/
eslint.config.mjs